# Conformance tools

`check-web.sh` runs the repository-owned web profile and compiler gates. Future
commands will acquire locked upstream suites and execute browser matrices
without changing the profile.
